\chapter{SYSTEM DESIGN}
\label{chap:system_design}

In the context of the AI-Driven Lab Exam Monitoring and Management System, system design marks a critical phase where the conceptual vision and requirements detailed in system analysis begin to take a structured, concrete, and implementable form. This stage focuses on thoughtfully outlining the software architecture of the entire platform, including its key components, modules, database wrappers, runtime execution blocks, and how they communicate with one another. It defines the technical blueprints of the platform and ensures that each part of the system aligns with student verification needs, invigilator supervision goals, academic grading standards, and strict system security constraints. By translating abstract user requirements and system boundaries into a tangible, organized framework, the system design phase serves as the technical foundation for a scalable, secure, concurrent, and maintainable software solution. Ultimately, system design bridges the gap between planning and development, guiding the project from logical definitions to physical prototypes.

\section{Structure Diagrams}
A structure diagram acts as a static blueprint that reveals how different components within a software system are organized, compiled, and connected. It provides a visual layout of the system’s architecture, making it straightforward to understand how each separate part contributes to the overall compilation and execution. In modern software engineering, structure diagrams illustrate the hierarchy, packaging, arrangement, and connections among database classes and physical servers, ensuring that they work together in a unified manner \cite{structure_diagram_techopedia}.

In the design of the SmartExam system, structure diagrams serve as essential tools for visualizing the static backbone of the application. They offer a high-level overview of how major database entities, service layers, controllers, client terminals, and remote database servers interact to deliver core functionalities like authentication, lockdown boundaries, and grading pipelines. By defining relationships and dependencies between compile-time components, structure diagrams streamline system design and ensure cohesive, concurrent development. Furthermore, by offering a clear and comprehensive depiction of the system's static framework, these models support effective communication among developers, administrators, and academic stakeholders, guiding the implementation of a stable, well-integrated prototype.

\subsection{Class Diagram}
A class diagram is a static UML structure diagram that outlines the structure of a system by showing its classes, their attributes, methods, and the relationships among objects \cite{class_diagram_uml, class_diagram_sparx}. It uses standardized block symbols to depict functional data entities, service logic controllers, and database access objects along with their fields and operations. These diagrams simplify complex object-oriented software by breaking it down into manageable components. Each class serves as a blueprint for instantiating runtime objects, ensuring code reuse, type safety, and encapsulation. Relationships between classes, including inheritance, associations, aggregations, dependencies, and cardinalities, are explicitly defined. This allows developers to anticipate structural bottlenecks, enforce object-oriented design patterns, and maintain referential integrity throughout the development lifecycle.

The class diagram for the SmartExam system is illustrated in Figure~\ref{fig:class_diagram}. It comprises the data models and repository layers that manage student accounts, physical workstation bindings, exam configurations, Whitelisted applications, seating structures, active telemetry events, and AI grading logs:

The class diagram shown in Figure~\ref{fig:class_diagram} details the properties and operations of the 14 core entity classes representing our domain model. The class model includes account entities (\texttt{User} and \texttt{Role}), hardware binding controllers (\texttt{DeviceBinding}), configuration descriptors (\texttt{Exam}, \texttt{Question}, \texttt{TestCase}, \texttt{AllowedApp}, and \texttt{SeatingMap}), active runtime state entities (\texttt{ExamSession} and \texttt{Answer}), background logs (\texttt{MonitoringEvent} and \texttt{AuditLog}), and post-exam evaluators (\texttt{AiGradingResult} and \texttt{PlagiarismResult}). Associations define relationships, such as the strict one-to-one relationship between a \texttt{User} and a \texttt{DeviceBinding} using motherboard/CPU HWID hashes. Additionally, the model outlines one-to-many associations linking an \texttt{Exam} to its whitelisted apps, questions, seating maps, and active sessions, structuring the flow of data within our relational database wrapper.

\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/class_diagram.png}
\caption{UML Class Diagram of the SmartExam Object Model}
\label{fig:class_diagram}
\end{figure}

\subsection{Deployment Diagrams}
A UML deployment diagram visually represents the physical architecture of a system, mapping out how software components are distributed across hardware devices, operating system nodes, and network segments during runtime. It shows where each executable component lives—whether it runs on a local workstation, a web browser, or a cloud server—and illustrates the communication protocols used to connect these nodes. This diagram is valuable for understanding how the physical infrastructure supports the system's operational requirements, highlighting the relationships between client devices, application servers, Docker hosts, and remote databases, which helps with resource allocation, security planning, scalability, and network maintenance.

The deployment configuration of the SmartExam system is shown in Figure~\ref{fig:deployment_diagram}, mapping out the runtime environments of the WPF student client, the React web dashboard, and the C\# ASP.NET Core backend:

The deployment topology mapped in Figure~\ref{fig:deployment_diagram} divides the platform into three main physical tiers. The Client tier includes the student workstation running the WPF desktop app, communicating with local Windows APIs for HWID retrieval and lockdown hooks. The Web tier includes the teacher's web browser rendering the React web panel. The Server tier includes the ASP.NET Core API server running on IIS or Kestrel, connected to a Neon PostgreSQL instance. Furthermore, the Server tier interfaces with a Docker Host that compiles and executes code submissions inside isolated sandboxes, and an AI Semantic Service that runs sentence-transformers models. Network links use HTTPS for REST API calls and WebSockets for streaming telemetry and live alerts, ensuring fast response times and robust security.

\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/deployment_diagram.png}
\caption{UML Deployment Diagram of the SmartExam Distributed Topology}
\label{fig:deployment_diagram}
\end{figure}

\subsection{Component Diagram}
A component diagram is a static UML structure diagram that depicts how software modules, APIs, and runtime wrappers are organized and wired together to form the complete physical system \cite{component_diagram_uml}. It describes the organization and wiring of the physical components in the system, showing the structural dependencies between components using interfaces. For the AI-Driven Lab Exam Monitoring System, the component diagram illustrates the static dependencies between the WPF Student Client application, the React Instructor Console web panel, the ASP.NET Core REST API backend server, and the external Docker sandbox compilation and sentence-transformers AI evaluation microservices. This modular packaging ensures a clear separation of concerns, where low-level Windows hooks, real-time WebSocket signals, and isolated compilation containers communicate through well-defined API boundaries. The component structure of the proposed system is shown in Figure~\ref{fig:component_diagram}.

\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/component_diagram.png}
\caption{Component Diagram of the Proposed System}
\label{fig:component_diagram}
\end{figure}

\section{Behavioral Diagrams}
Behavioral diagrams serve as dynamic blueprints that illustrate how a software system operates during its execution, showing how data moves, how states change, and how objects interact. These diagrams capture the interactions between system components, human actors, and external APIs, showing how information pipelines are executed \cite{behavioral_uml_diagrams}. They are essential for understanding the runtime functionality of software. For example, activity diagrams trace the control flow within a process, sequence diagrams map chronological message passing, and communication diagrams highlight collaborating objects.

Behavioral diagrams are valuable for analyzing the dynamic properties of the SmartExam platform. By illustrating how real-time data moves and how different components coordinate over time, they help identify potential latency issues, optimize data streaming, and verify that the system behaves correctly under concurrent conditions.

\subsection{Activity Diagrams}
An activity diagram in UML is a specialized flowchart that models the dynamic flow of control and activities within a system. Unlike simple flowcharts, activity diagrams capture granular processes, decision points, concurrent operations, and loops. They show how actions depend on one another, using swimlanes to separate responsibilities. By clearly showing how tasks are triggered, how conditions affect execution paths, and how final states are reached, activity diagrams help developers visualize business logic and user workflows \cite{activity_diagram_vp}.

In the SmartExam platform, we define exactly 14 activity diagrams corresponding to the 14 core functional requirements:

\subsubsection{ManageUserAccounts}
This activity diagram tracks the administrator workflow for user account configurations, showing the decision path for creating, modifying, or disabling student and instructor accounts.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_manage_user_accounts.png}
\caption{Activity Diagram for ManageUserAccounts}
\label{fig:act_manage_user_accounts}
\end{figure}

\subsubsection{AuthenticateUserSession}
This activity diagram shows the login validation flow, branching based on credential verification outcomes.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_authenticate_user_session.png}
\caption{Activity Diagram for AuthenticateUserSession}
\label{fig:act_authenticate_user_session}
\end{figure}

\subsubsection{VerifyWorkstationHardware}
This activity diagram captures the automated Hardware ID (HWID) verification process, showing the decision branch for binding validation and lockout overrides.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_verify_workstation_hardware.png}
\caption{Activity Diagram for VerifyWorkstationHardware}
\label{fig:act_verify_workstation_hardware}
\end{figure}

\subsubsection{CreateExamTemplate}
This activity diagram models the exam creation workflow, including question addition and compiler test case configuration.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_create_exam_template.png}
\caption{Activity Diagram for CreateExamTemplate}
\label{fig:act_create_exam_template}
\end{figure}

\subsubsection{ConfigureApplicationWhitelist}
This activity diagram maps the configuration of permitted process whitelists, showing validation paths for process paths and extensions.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_configure_application_whitelist.png}
\caption{Activity Diagram for ConfigureApplicationWhitelist}
\label{fig:act_configure_application_whitelist}
\end{figure}

\subsubsection{MapSeatingEligibility}
This activity diagram traces seating layouts and IP range allocation, illustrating collision checks and verification pathways.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_map_seating_eligibility.png}
\caption{Activity Diagram for MapSeatingEligibility}
\label{fig:act_map_seating_eligibility}
\end{figure}

\subsubsection{ExecutePreExamDiagnostics}
This activity diagram traces the pre-exam system verification checks, looping over webcam, mic, processes, and network checks.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_execute_pre_exam_diagnostics.png}
\caption{Activity Diagram for ExecutePreExamDiagnostics}
\label{fig:act_execute_pre_exam_diagnostics}
\end{figure}

\subsubsection{DisplayStudentDashboard}
This activity diagram illustrates candidate dashboard loading, checking exam schedules and enabling workstation launcher components.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_display_student_dashboard.png}
\caption{Activity Diagram for DisplayStudentDashboard}
\label{fig:act_display_student_dashboard}
\end{figure}

\subsubsection{LockdownStudentWorkstation}
This activity diagram details WPF workstation lockdown operations, showing keyboard hooks activation and display restrictions.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_lockdown_student_workstation.png}
\caption{Activity Diagram for LockdownStudentWorkstation}
\label{fig:act_lockdown_student_workstation}
\end{figure}

\subsubsection{ConductExamSession}
This activity diagram maps student activities inside the locked workspace, including inputs, compile runs, and local caching.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_conduct_exam_session.png}
\caption{Activity Diagram for ConductExamSession}
\label{fig:act_conduct_exam_session}
\end{figure}

\subsubsection{StreamActivityTelemetry}
This activity diagram models the background logging and periodic transmission loops of telemetry data over WebSockets.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_stream_activity_telemetry.png}
\caption{Activity Diagram for StreamActivityTelemetry}
\label{fig:act_stream_activity_telemetry}
\end{figure}

\subsubsection{MonitorLiveClassroom}
This activity diagram models live proctoring supervision, detailing event monitoring alerts and remote command overrides.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_monitor_live_classroom.png}
\caption{Activity Diagram for MonitorLiveClassroom}
\label{fig:act_monitor_live_classroom}
\end{figure}

\subsubsection{SubmitExamAnswers}
This activity diagram shows the submission sequence, checking network presence and closing client hooks.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_submit_exam_answers.png}
\caption{Activity Diagram for SubmitExamAnswers}
\label{fig:act_submit_exam_answers}
\end{figure}

\subsubsection{EvaluateExamSubmissions}
This activity diagram outlines the grading and reporting flows, compiling code, calling AI semantic checkers, and downloading reports.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/act_evaluate_exam_submissions.png}
\caption{Activity Diagram for EvaluateExamSubmissions}
\label{fig:act_evaluate_exam_submissions}
\end{figure}

\subsection{Communication Diagrams}
UML communication diagrams provide a structured way to visualize object interactions within a system by focusing on the links and messages exchanged between objects. While similar to sequence diagrams, communication diagrams emphasize the structural relationships and routing paths among objects rather than their timeline sequence. Objects are connected by association links, and numbered arrows indicate the sequence and direction of message passing. These diagrams show how multiple components collaborate to fulfill a specific use case, helping developers verify object responsibilities and ensure that collaboration pathways are logically structured \cite{communication_diagram_boardmix}.

The collaboration of objects during a live proctoring override event is illustrated in Figure~\ref{fig:communication_diagram}:

The communication model in Figure~\ref{fig:communication_diagram} shows how messages are passed between the WPF student client, the WebSocket hub, the database, and the teacher's proctoring dashboard during an override action. Message 1 represents the student client streaming a focus-loss alert to the WebSocket hub. Message 2 represents the hub writing this event to the database. Message 3 represents the hub pushing a real-time WebSocket warning to the teacher's proctoring controller, which displays an alert. Message 4 represents the teacher initiating an override command (e.g., pausing the exam timer). Message 5 represents the dashboard sending an API request to the override controller, which updates the session state in the database (Message 6) and sends a remote pause signal to the student client (Message 7).

\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/communication_diagram.png}
\caption{UML Communication Diagram of real-time Telemetry Alerting and Teacher Overrides}
\label{fig:communication_diagram}
\end{figure}

\subsection{Sequence Diagrams}
UML sequence diagrams are used to visualize the chronological flow of events and messages within a system, illustrating how components interact over time. They provide a timeline-based view, with lifelines representing participating objects and horizontal arrows representing messages or method calls. This structure helps developers track execution flows, trace API requests, identify latency bottlenecks, and design secure authentication patterns \cite{sequence_diagram_medium}.

In the SmartExam platform, exactly 14 sequence diagrams map the chronological communication between client interfaces, system modules, and database structures. Each diagram maps the interactions between the User (actor), the specific Module Name (controller/wrapper), and the Database (Neon PostgreSQL) along a chronological timeline. Alternatives (`alt` fragments) manage exception paths, and loops (`loop` fragments) handle repetitive processes.

\subsubsection{ManageUserAccounts}
This sequence diagram tracks account configurations on a timeline. The lifeline objects are the Admin (User), the AdminController (Module Name), and Neon PostgreSQL (Database). An `alt` fragment separates successful updates from database constraint errors.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_manage_user_accounts.png}
\caption{Sequence Diagram for ManageUserAccounts}
\label{fig:seq_manage_user_accounts}
\end{figure}

\subsubsection{AuthenticateUserSession}
This sequence diagram captures credential validation. The lifelines are the User (Student/Teacher/Admin), the AuthController (Module Name), and Neon PostgreSQL (Database). The timeline shows credential post, password verification using BCrypt, and token generation within `alt` blocks.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_authenticate_user_session.png}
\caption{Sequence Diagram for AuthenticateUserSession}
\label{fig:seq_authenticate_user_session}
\end{figure}

\subsubsection{VerifyWorkstationHardware}
This sequence diagram maps device binding verification. The lifelines are the Student (User), the WPF Device Binder (Module Name), and Neon PostgreSQL (Database). The timeline traces hardware hash retrieval, with `alt` checking if bounds match.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_verify_workstation_hardware.png}
\caption{Sequence Diagram for VerifyWorkstationHardware}
\label{fig:seq_verify_workstation_hardware}
\end{figure}

\subsubsection{CreateExamTemplate}
This sequence diagram maps exam template creation. The lifelines are the Teacher (User), the ExamConfigController (Module Name), and Neon PostgreSQL (Database). A `loop` block handles adding multiple questions and compiling test cases.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_create_exam_template.png}
\caption{Sequence Diagram for CreateExamTemplate}
\label{fig:seq_create_exam_template}
\end{figure}

\subsubsection{ConfigureApplicationWhitelist}
This sequence diagram outlines whitelist process creation. The lifelines are the Teacher (User), the WhitelistController (Module Name), and Neon PostgreSQL (Database). An `alt` block checks application extensions before updating rules.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_configure_application_whitelist.png}
\caption{Sequence Diagram for ConfigureApplicationWhitelist}
\label{fig:seq_configure_application_whitelist}
\end{figure}

\subsubsection{MapSeatingEligibility}
This sequence diagram maps seating and access controls. The lifelines are the Teacher (User), the SeatingController (Module Name), and Neon PostgreSQL (Database). The timeline schedules terminals, with `alt` checking seating overlaps.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_map_seating_eligibility.png}
\caption{Sequence Diagram for MapSeatingEligibility}
\label{fig:seq_map_seating_eligibility}
\end{figure}

\subsubsection{ExecutePreExamDiagnostics}
This sequence diagram tracks diagnostic scans. The lifelines are the Student (User), the DiagnosticsController (Module Name), and Neon PostgreSQL (Database). A `loop` iterates over camera and process scans, reporting to the database.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_execute_pre_exam_diagnostics.png}
\caption{Sequence Diagram for ExecutePreExamDiagnostics}
\label{fig:seq_execute_pre_exam_diagnostics}
\end{figure}

\subsubsection{DisplayStudentDashboard}
This sequence diagram details student dashboard rendering. The lifelines are the Student (User), the StudentDashboard (Module Name), and Neon PostgreSQL (Database). Active exams are fetched on a timeline, and `alt` checks for active states.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_display_student_dashboard.png}
\caption{Sequence Diagram for DisplayStudentDashboard}
\label{fig:seq_display_student_dashboard}
\end{figure}

\subsubsection{LockdownStudentWorkstation}
This sequence diagram maps WPF workstation lockdown. The lifelines are the Student (User), the WPF Lockdown Manager (Module Name), and Neon PostgreSQL (Database). The manager locks keyboard hooks and secondary displays on the timeline.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_lockdown_student_workstation.png}
\caption{Sequence Diagram for LockdownStudentWorkstation}
\label{fig:seq_lockdown_student_workstation}
\end{figure}

\subsubsection{ConductExamSession}
This sequence diagram tracks candidate actions in the workspace. The lifelines are the Student (User), the ExamWorkspace (Module Name), and Neon PostgreSQL (Database). In a `loop`, student answers are typed, and mock compilations are executed.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_conduct_exam_session.png}
\caption{Sequence Diagram for ConductExamSession}
\label{fig:seq_conduct_exam_session}
\end{figure}

\subsubsection{StreamActivityTelemetry}
This sequence diagram maps WebSocket telemetry. The lifelines are the Student (User), the SignalR telemetry hub (Module Name), and Neon PostgreSQL (Database). A `loop` collects and streams screenshots and active processes to database logs.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_stream_activity_telemetry.png}
\caption{Sequence Diagram for StreamActivityTelemetry}
\label{fig:seq_stream_activity_telemetry}
\end{figure}

\subsubsection{MonitorLiveClassroom}
This sequence diagram details teacher proctoring. The lifelines are the Teacher (User), the ProctoringConsole (Module Name), and Neon PostgreSQL (Database). Pushed signals are rendered in a `loop`, and `alt` handles manual override events.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_monitor_live_classroom.png}
\caption{Sequence Diagram for MonitorLiveClassroom}
\label{fig:seq_monitor_live_classroom}
\end{figure}

\subsubsection{SubmitExamAnswers}
This sequence diagram captures submission control. The lifelines are the Student (User), the SubmitController (Module Name), and Neon PostgreSQL (Database). The final sync runs, and `alt` releases keyboard hooks or writes to cache if offline.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_submit_exam_answers.png}
\caption{Sequence Diagram for SubmitExamAnswers}
\label{fig:seq_submit_exam_answers}
\end{figure}

\subsubsection{EvaluateExamSubmissions}
This sequence diagram maps evaluations. The lifelines are the Teacher (User), the GradingController (Module Name), and Neon PostgreSQL (Database). Submissions are compiled in Docker, AI semantic similarity scored, and PDF reports generated on the timeline.
\begin{figure}[H]
\centering
\includegraphics[width=0.97\linewidth]{Figures/seq_evaluate_exam_submissions.png}
\caption{Sequence Diagram for EvaluateExamSubmissions}
\label{fig:seq_evaluate_exam_submissions}
\end{figure}